{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'serverless functions only, no build needed'",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/health", "destination": "/api/index" }
  ]
}
