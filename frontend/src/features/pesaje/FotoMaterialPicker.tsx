/** Selector de fotos de un material — vive en `components/FotoMultiplePicker`
 *  (reutilizado en todo el sistema, no solo pesaje); este archivo se
 *  mantiene como re-export para no tocar los imports existentes. */
export { default } from '../../components/FotoMultiplePicker';
